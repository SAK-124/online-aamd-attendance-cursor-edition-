[build]
  base = "frontend"             # where your package.json is
  publish = "frontend/dist"     # the build output folder (Vite default)
  command = "npm ci && npm run build"

[functions]
  directory = "netlify/functions"
